class Solution:
    # Recursive
    # def fib(self, n: int, memo={}):
    #     if n in memo: return memo[n]
    #     if n <= 0: return 0
    #     elif n <= 1: return 1
    #     memo[n] = self.fib(n - 1, memo) + self.fib(n - 2, memo)
    #     return memo[n]

    # Iterative
    def fib(self, n: int): -> int
        arr = []
    


print(Solution().fib(5))
